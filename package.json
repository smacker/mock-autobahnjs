{
  "name": "mock-autobahnjs",
  "version": "0.1.0",
  "description": "Wrapper around mock-socket to work with autobahnjs",
  "main": "dist/index.js",
  "scripts": {
    "lint": "eslint .",
    "build": "babel -d dist src/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Maxim Sukharev",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-stage-2": "^6.5.0",
    "eslint": "^2.3.0"
  },
  "dependencies": {
    "mock-socket": "^2.0.0"
  }
}
